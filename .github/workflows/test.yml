name: Test DOU Bot

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          python -m playwright install chromium

      - name: Run unit tests
        run: |
          echo "🧪 Testando estrutura do projeto..."
          
          # Testar imports
          python -c "
          from core.config import Config
          from core.state import StateManager
          from models.publication import Publication
          print('✅ Imports funcionando')
          "
          
          # Testar configuração YAML
          python -c "
          import yaml
          with open('config.yml', 'r') as f:
              config = yaml.safe_load(f)
          assert 'search' in config
          assert 'email' in config
          print('✅ config.yml válido')
          "
          
          # Testar estrutura de pastas
          python -c "
          import os
          required_dirs = ['src/core', 'src/scraper', 'src/ai', 'src/email', 'src/models', 'src/utils']
          for dir in required_dirs:
              assert os.path.exists(dir), f'Diretório {dir} não existe'
          print('✅ Estrutura de pastas OK')
          "

      - name: Test email templates
        run: |
          echo "📧 Testando templates de email..."
          python -c "
          from email.templates import EmailTemplates
          templates = EmailTemplates.create_test_email()
          assert 'subject' in templates
          assert 'html' in templates
          assert 'text' in templates
          print('✅ Templates funcionando')
          "

      - name: Validate requirements.txt
        run: |
          echo "📦 Validando requirements.txt..."
          pip install -r requirements.txt --dry-run
          echo "✅ requirements.txt válido"
